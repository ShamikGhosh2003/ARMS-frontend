# Airline Frontend


This is a Vite + React + Tailwind frontend for your Spring Boot backend.


## Setup
1. Install deps: `npm install`
2. Start dev server: `npm run dev`
3. Ensure backend runs at `http://localhost:8080` or set `VITE_API_BASE` in `.env`